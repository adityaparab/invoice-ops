apiVersion: 1

datasources:
  - name: InvoiceOps Prometheus
    uid: invoiceops-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
